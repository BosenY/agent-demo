# Agent Demo 09-13 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 agent-demo 项目新增 5 个独立 demo 文件（09-13），分别演示动态技能开关、MCP Server、MCP Client、跨会话记忆、Agent 心跳监控。

**Architecture:** 每个 demo 是一个自包含的 `.ts` 文件，直接通过 `bun <file>` 运行，复用 `lib/ai.ts` 的 model 实例，无共享状态。Demo 10 是独立 MCP Server 进程，Demo 11 通过 `Experimental_StdioMCPTransport` 自动 fork Demo 10 子进程。

**Tech Stack:** Bun、Vercel AI SDK v4.3.19、@modelcontextprotocol/sdk、TypeScript、zod

---

## Task 0: 安装依赖

**Files:**
- Modify: `package.json`
- Modify: `bun.lock`（自动更新）

- [ ] **Step 1: 安装 `@modelcontextprotocol/sdk`**

```bash
pnpm add @modelcontextprotocol/sdk
```

Expected output: 包含 `@modelcontextprotocol/sdk` 的 `+ <version>` 行，无 error。

- [ ] **Step 2: 验证安装**

```bash
ls node_modules/@modelcontextprotocol/sdk/package.json
```

Expected: 文件存在，打印路径。

---

## Task 1: Demo 09 — 动态技能开关

**Files:**
- Create: `demo-09-dynamic-skills.ts`

### 背景知识

`experimental_activeTools` 是一个字符串数组，列出当前步骤模型可以调用的工具名称。通过在 `onStepFinish` 中修改该数组引用，可以在每步之间动态切换激活的工具子集。注意：需要将 `activeTools` 变量声明为可变引用，并在 `generateText` 参数中每次读取最新值——但由于 Vercel AI SDK 在调用时已捕获引用，实际上需要用对象包装来确保动态性。

### 实现说明

Vercel AI SDK 的 `experimental_activeTools` 在每次 step 开始时都会重新读取（它是在 step 循环内部引用的），因此直接修改数组内容（`splice` + `push`）即可生效。

- [ ] **Step 1: 创建文件**

创建 `demo-09-dynamic-skills.ts`，内容如下：

```ts
// 练习目标：用 experimental_activeTools 动态切换每步可用的工具子集
import { generateText, tool } from "ai"
import { z } from "zod"
import { model } from "./lib/ai"

const activeTools: string[] = ["classify"]

const jokes = ["为什么程序员喜欢暗色主题？因为 light 会吸引 bug。", "HTTP 418：我是一个茶壶。"]

const result = await generateText({
  model,
  prompt: "帮我算一下 99 * 88 等于多少？",
  tools: {
    classify: tool({
      description: "判断用户问题的类型，返回 math、time 或 joke 之一",
      parameters: z.object({
        category: z.enum(["math", "time", "joke"]).describe("问题类型"),
      }),
      execute: async ({ category }) => category,
    }),
    calculate: tool({
      description: "执行数学表达式，返回计算结果",
      parameters: z.object({
        expression: z.string().describe("合法的 JS 数学表达式，例如 99 * 88"),
      }),
      execute: async ({ expression }) =>
        String(Function(`"use strict"; return (${expression})`)()),
    }),
    get_time: tool({
      description: "返回当前北京时间",
      parameters: z.object({}),
      execute: async () =>
        new Date().toLocaleString("zh-CN", { timeZone: "Asia/Shanghai" }),
    }),
    get_joke: tool({
      description: "返回一条程序员笑话",
      parameters: z.object({}),
      execute: async () => jokes[Math.floor(Math.random() * jokes.length)],
    }),
  },
  experimental_activeTools: activeTools,
  maxSteps: 3,
  onStepFinish: ({ toolResults }) => {
    const category = toolResults[0]?.result as string | undefined
    activeTools.length = 0
    if (category === "math") activeTools.push("calculate")
    else if (category === "time") activeTools.push("get_time")
    else if (category === "joke") activeTools.push("get_joke")
    if (category) console.log(`\n[路由] 分类结果: ${category} → 激活工具: ${activeTools.join(", ")}`)
  },
})

console.log("\n=== 最终回复 ===")
console.log(result.text)
console.log(`\n共执行了 ${result.steps.length} 步`)
```

- [ ] **Step 2: 运行验证**

```bash
bun demo-09-dynamic-skills.ts
```

Expected: 输出 `[路由] 分类结果: math → 激活工具: calculate`，最终回复包含 `8712`（99×88 的结果）。

- [ ] **Step 3: 换一个问题验证 joke 分支**

临时把 prompt 改为 `"讲个笑话吧"`，运行后检查输出：

```bash
bun -e "
import { generateText, tool } from 'ai'
import { z } from 'zod'
import { model } from './lib/ai'
const activeTools = ['classify']
const jokes = ['为什么程序员喜欢暗色主题？因为 light 会吸引 bug。']
const r = await generateText({
  model, prompt: '讲个笑话吧',
  tools: {
    classify: tool({ description: '判断问题类型', parameters: z.object({ category: z.enum(['math','time','joke']) }), execute: async ({category}) => category }),
    get_joke: tool({ description: '返回笑话', parameters: z.object({}), execute: async () => jokes[0] }),
    calculate: tool({ description: '计算', parameters: z.object({ expression: z.string() }), execute: async ({expression}) => String(Function('return (' + expression + ')')()) }),
    get_time: tool({ description: '时间', parameters: z.object({}), execute: async () => new Date().toLocaleString() }),
  },
  experimental_activeTools: activeTools, maxSteps: 3,
  onStepFinish: ({ toolResults }) => { const c = toolResults[0]?.result; activeTools.length = 0; if(c==='joke') activeTools.push('get_joke'); else if(c==='math') activeTools.push('calculate'); else if(c==='time') activeTools.push('get_time'); if(c) console.log('[路由]', c, '->', activeTools) }
})
console.log(r.text)
"
```

Expected: 路由到 `joke`，最终返回笑话文本。

- [ ] **Step 4: 恢复原始 prompt 并确认文件正确**

确认 `demo-09-dynamic-skills.ts` 中的 prompt 是 `"帮我算一下 99 * 88 等于多少？"`。

---

## Task 2: Demo 10 — MCP Server

**Files:**
- Create: `demo-10-mcp-server.ts`

### 背景知识

`@modelcontextprotocol/sdk` 的 `McpServer` 是高层封装，`server.tool(name, schema, handler)` 注册工具。`StdioServerTransport` 通过 stdin/stdout 与 MCP Client 通信。Server 启动后不退出，等待 Client 请求。

- [ ] **Step 1: 创建文件**

创建 `demo-10-mcp-server.ts`，内容如下：

```ts
// MCP Server：通过 stdio 暴露 3 个工具供 MCP Client 调用
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js"
import { z } from "zod"

const jokes: Record<string, string> = {
  general: "为什么程序员喜欢暗色主题？因为 light 会吸引 bug。",
  http: "HTTP 418：我是一个茶壶。",
  git: "Git blame 的最大作用是让你记住：六个月前的你也是别人。",
}

const server = new McpServer({ name: "agent-demo-server", version: "1.0.0" })

server.tool(
  "get_time",
  "返回指定时区的当前时间",
  { timezone: z.string().optional().describe("时区名称，例如 Asia/Shanghai") },
  async ({ timezone }) => ({
    content: [
      {
        type: "text" as const,
        text: new Date().toLocaleString("zh-CN", {
          timeZone: timezone ?? "Asia/Shanghai",
        }),
      },
    ],
  }),
)

server.tool(
  "calculate",
  "执行数学表达式并返回结果",
  { expression: z.string().describe("合法的 JS 数学表达式") },
  async ({ expression }) => {
    try {
      const result = Function(`"use strict"; return (${expression})`)()
      return { content: [{ type: "text" as const, text: String(result) }] }
    } catch (e) {
      return { content: [{ type: "text" as const, text: `错误: ${e}` }] }
    }
  },
)

server.tool(
  "get_joke",
  "返回一条程序员笑话",
  { topic: z.string().optional().describe("话题：general、http、git") },
  async ({ topic }) => ({
    content: [
      {
        type: "text" as const,
        text: jokes[topic ?? "general"] ?? jokes.general,
      },
    ],
  }),
)

await server.connect(new StdioServerTransport())
```

- [ ] **Step 2: 检查 MCP SDK 导入路径**

```bash
ls node_modules/@modelcontextprotocol/sdk/server/
```

Expected: 包含 `mcp.js`（或 `mcp.d.ts`）和 `stdio.js`（或 `stdio.d.ts`）。

若路径不同（如 `dist/server/`），调整 import 路径：

```bash
find node_modules/@modelcontextprotocol/sdk -name "mcp.js" | head -5
find node_modules/@modelcontextprotocol/sdk -name "stdio.js" | head -5
```

- [ ] **Step 3: 验证 Server 可启动**

在一个终端运行（5 秒后 Ctrl+C 退出）：

```bash
bun demo-10-mcp-server.ts
```

Expected: 进程启动后没有立即报错或退出，静默等待输入（MCP Server 正常状态）。

---

## Task 3: Demo 11 — MCP Client

**Files:**
- Create: `demo-11-mcp-client.ts`

### 背景知识

`experimental_createMCPClient` 从 `ai` 包导出（别名）。`Experimental_StdioMCPTransport` 从 `ai/mcp-stdio` 导出，它会通过 `child_process.spawn` fork 指定命令作为 MCP Server 子进程。`client.tools()` 返回已转换为 AI SDK tool 格式的工具集，可直接传给 `generateText`。

- [ ] **Step 1: 创建文件**

创建 `demo-11-mcp-client.ts`，内容如下：

```ts
// 练习目标：用 MCP Client 连接本地 MCP Server，工具自动透传给模型
import { experimental_createMCPClient, generateText } from "ai"
import { Experimental_StdioMCPTransport } from "ai/mcp-stdio"
import { model } from "./lib/ai"

const client = await experimental_createMCPClient({
  transport: new Experimental_StdioMCPTransport({
    command: "bun",
    args: ["demo-10-mcp-server.ts"],
  }),
})

console.log("MCP Client 已连接，正在获取工具列表...")
const mcpTools = await client.tools()
console.log("已加载工具:", Object.keys(mcpTools).join(", "))

const result = await generateText({
  model,
  tools: mcpTools,
  prompt: "告诉我现在几点、帮我计算 123 * 456、再讲一个 http 话题的笑话",
  maxSteps: 5,
})

console.log("\n=== 工具调用记录 ===")
result.steps.forEach((step, i) => {
  if (step.toolCalls.length > 0) {
    console.log(`Step ${i + 1}:`, step.toolCalls.map(c => c.toolName).join(", "))
  }
})

console.log("\n=== 最终回复 ===")
console.log(result.text)

await client.close()
```

- [ ] **Step 2: 运行验证**

```bash
bun demo-11-mcp-client.ts
```

Expected:
- 打印 `已加载工具: get_time, calculate, get_joke`（顺序可能不同）
- 步骤记录中出现 `get_time`、`calculate`、`get_joke` 至少各一次
- 最终回复包含时间、`56088`（123×456）、笑话内容

---

## Task 4: Demo 12 — 跨会话记忆

**Files:**
- Create: `demo-12-memory.ts`

### 背景知识

参考 OpenCode 的 session 持久化方案：以 session 为单位将完整 `CoreMessage[]` 序列化为 JSON 文件，每轮对话后立即写盘。启动时扫描 `./sessions/` 找最近文件，提示用户选择恢复或新建。使用 Bun 原生文件 API（`Bun.file`、`Bun.write`）进行读写，`node:readline` 实现交互 REPL。

- [ ] **Step 1: 创建文件**

创建 `demo-12-memory.ts`，内容如下：

```ts
// 练习目标：跨会话持久化记忆——对话历史写入 JSON 文件，下次启动可恢复
import { generateText, type CoreMessage } from "ai"
import { createInterface } from "node:readline"
import { existsSync, mkdirSync, readdirSync, statSync } from "node:fs"
import { join } from "node:path"
import { model } from "./lib/ai"

interface Session {
  id: string
  createdAt: string
  messages: CoreMessage[]
}

const sessionsDir = "./sessions"
if (!existsSync(sessionsDir)) mkdirSync(sessionsDir)

function shortId(): string {
  return Math.random().toString(36).slice(2, 8)
}

function findLatestSession(): { path: string; session: Session } | null {
  const files = readdirSync(sessionsDir)
    .filter(f => f.endsWith(".json"))
    .map(f => ({ name: f, path: join(sessionsDir, f), mtime: statSync(join(sessionsDir, f)).mtime }))
    .sort((a, b) => b.mtime.getTime() - a.mtime.getTime())

  if (files.length === 0) return null
  const latest = files[0]
  const session: Session = JSON.parse(Bun.file(latest.path).toString())
  return { path: latest.path, session }
}

function minutesAgo(isoDate: string): string {
  const mins = Math.floor((Date.now() - new Date(isoDate).getTime()) / 60000)
  if (mins < 1) return "刚刚"
  if (mins < 60) return `${mins} 分钟前`
  return `${Math.floor(mins / 60)} 小时前`
}

async function ask(rl: ReturnType<typeof createInterface>, q: string): Promise<string> {
  return new Promise(resolve => rl.question(q, resolve))
}

const rl = createInterface({ input: process.stdin, output: process.stdout })

let sessionPath: string
let messages: CoreMessage[]

const latest = findLatestSession()
if (latest) {
  const { session } = latest
  const msgCount = session.messages.length
  const ago = minutesAgo(session.createdAt)
  const answer = await ask(rl, `上次对话（${msgCount} 条消息，${ago}）—— 继续？[y/N] `)
  if (answer.trim().toLowerCase() === "y") {
    messages = session.messages
    sessionPath = latest.path
    console.log(`已加载会话 ${session.id}，历史 ${msgCount} 条消息\n`)
  } else {
    const id = `${Date.now()}-${shortId()}`
    sessionPath = join(sessionsDir, `${id}.json`)
    messages = []
    console.log(`新建会话 ${id}\n`)
  }
} else {
  const id = `${Date.now()}-${shortId()}`
  sessionPath = join(sessionsDir, `${id}.json`)
  messages = []
  console.log("首次运行，新建会话\n")
}

async function saveSession(): Promise<void> {
  const id = sessionPath.split("/").pop()!.replace(".json", "")
  const session: Session = {
    id,
    createdAt: new Date().toISOString(),
    messages,
  }
  await Bun.write(sessionPath, JSON.stringify(session, null, 2))
}

console.log('输入消息开始对话，输入 "exit" 退出\n')

while (true) {
  const input = await ask(rl, "你: ")
  if (input.trim().toLowerCase() === "exit") break

  messages.push({ role: "user", content: input })

  const result = await generateText({ model, messages })
  messages.push({ role: "assistant", content: result.text })

  console.log(`\n助手: ${result.text}\n`)
  await saveSession()
}

rl.close()
console.log(`\n会话已保存到 ${sessionPath}`)
console.log(`共 ${messages.length} 条消息`)
```

- [ ] **Step 2: 运行 — 首次对话**

```bash
bun demo-12-memory.ts
```

交互步骤：
1. 因为 `./sessions/` 为空，提示 `首次运行，新建会话`
2. 输入：`你好，我叫新语`
3. 等待回复后输入 `exit`

Expected: 打印会话保存路径，`./sessions/` 下出现 `.json` 文件。

- [ ] **Step 3: 验证持久化**

```bash
ls ./sessions/
cat ./sessions/*.json | head -30
```

Expected: JSON 文件存在，`messages` 数组包含 user 和 assistant 各一条消息。

- [ ] **Step 4: 运行 — 恢复会话**

```bash
bun demo-12-memory.ts
```

交互步骤：
1. 看到 `上次对话（2 条消息，...）—— 继续？[y/N]` 提示
2. 输入 `y`
3. 输入：`你还记得我叫什么名字吗？`
4. 验证模型回答包含「新语」
5. 输入 `exit`

Expected: 模型能正确引用上轮会话中的名字，说明历史记忆加载成功。

---

## Task 5: Demo 13 — Agent 心跳监控

**Files:**
- Create: `demo-13-heartbeat.ts`

### 背景知识

`onStepFinish` 在每个 Agent step 完成后同步调用，参数包含 `toolCalls`、`toolResults`、`usage`（含 `totalTokens`）、`finishReason`。`process.stdout.write("\r" + line)` 可在终端原地覆盖当前行，实现实时刷新效果。

- [ ] **Step 1: 创建文件**

创建 `demo-13-heartbeat.ts`，内容如下：

```ts
// 练习目标：onStepFinish 心跳监控——每步实时上报执行进度与 token 用量
import { generateText, tool } from "ai"
import { z } from "zod"
import { model } from "./lib/ai"

interface Heartbeat {
  step: number
  elapsed: string
  tokensUsed: number
  status: string
  lastTool?: string
}

const fakeData: Record<string, string> = {
  量子计算: "量子计算利用量子叠加和纠缠特性，理论上可并行处理经典计算机无法解决的复杂问题。目前 IBM、Google 等已实现百量子比特级别的量子处理器。",
  现状: "2024年量子计算进入NISQ（含噪声中等规模量子）时代，量子优越性在特定任务上已被证明，但通用容错量子计算机仍需数年研发。",
}

const startTime = Date.now()
let stepIndex = 0
let totalTokens = 0

function printHeartbeat(beat: Heartbeat): void {
  const tool = beat.lastTool ? ` | 工具: ${beat.lastTool}` : ""
  const line = `[心跳] Step ${beat.step} | 耗时 ${beat.elapsed} | Tokens ${beat.tokensUsed} | ${beat.status}${tool}`
  process.stdout.write(`\r${line.padEnd(80)}`)
}

const result = await generateText({
  model,
  prompt: "请研究「量子计算的现状」：先用 search 搜索「量子计算」，再用 analyze 分析搜索结果，最后给出总结。",
  tools: {
    search: tool({
      description: "搜索指定关键词，返回相关信息",
      parameters: z.object({
        query: z.string().describe("搜索关键词"),
      }),
      execute: async ({ query }) => {
        await new Promise(r => setTimeout(r, 500))
        return fakeData[query] ?? `关于「${query}」暂无数据，请尝试其他关键词。`
      },
    }),
    analyze: tool({
      description: "分析给定文本，提取关键信息点",
      parameters: z.object({
        content: z.string().describe("需要分析的文本内容"),
      }),
      execute: async ({ content }) => {
        await new Promise(r => setTimeout(r, 300))
        const points = content.split("，").slice(0, 3).map((p, i) => `${i + 1}. ${p.trim()}`)
        return `关键要点：\n${points.join("\n")}`
      },
    }),
  },
  maxSteps: 6,
  onStepFinish: ({ toolCalls, toolResults, usage, finishReason }) => {
    totalTokens += usage?.totalTokens ?? 0
    const beat: Heartbeat = {
      step: ++stepIndex,
      elapsed: `${((Date.now() - startTime) / 1000).toFixed(1)}s`,
      tokensUsed: totalTokens,
      status: finishReason === "tool-calls" ? "工具调用中" : finishReason === "stop" ? "推理完成" : "生成中",
      lastTool: toolCalls[0]?.toolName,
    }
    printHeartbeat(beat)
  },
})

process.stdout.write("\n")
console.log("\n=== Agent 执行摘要 ===")
console.log(`总步骤: ${result.steps.length}`)
console.log(`总耗时: ${((Date.now() - startTime) / 1000).toFixed(1)}s`)
console.log(`总 Tokens: ${totalTokens}`)

console.log("\n=== 最终结果 ===")
console.log(result.text)
```

- [ ] **Step 2: 运行验证**

```bash
bun demo-13-heartbeat.ts
```

Expected:
- 运行过程中终端同一行持续刷新心跳信息，类似：
  ```
  [心跳] Step 2 | 耗时 1.8s | Tokens 312 | 工具调用中 | 工具: search
  ```
- 完成后输出执行摘要（总步骤 ≥ 3）和最终结果

---

## Task 6: 更新 README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 在 Demo 列表表格末尾追加 5 行**

在 `README.md` 的 Demo 列表表格中，找到 `demo-08-multi-provider.ts` 那行，在其后追加：

```markdown
| `demo-09-dynamic-skills.ts` | `experimental_activeTools` 动态技能路由，分类后激活对应工具子集 |
| `demo-10-mcp-server.ts` | `@modelcontextprotocol/sdk` 搭建本地 MCP Server，stdio 传输 |
| `demo-11-mcp-client.ts` | `experimental_createMCPClient` 连接 MCP Server，工具自动透传 |
| `demo-12-memory.ts` | 跨会话 JSON 持久化记忆，readline REPL，启动时可恢复历史 |
| `demo-13-heartbeat.ts` | `onStepFinish` 心跳监控，终端原地刷新步骤进度与 token 用量 |
```

- [ ] **Step 2: 在运行说明部分追加**

找到 `## 运行` 部分，在最后一行 `bun demo-08-multi-provider.ts` 后追加：

```markdown
bun demo-09-dynamic-skills.ts
bun demo-10-mcp-server.ts       # 单独运行可测试 MCP Server（Ctrl+C 退出）
bun demo-11-mcp-client.ts       # 自动启动 demo-10 子进程，无需手动启动
bun demo-12-memory.ts           # 交互式 REPL，输入 exit 退出
bun demo-13-heartbeat.ts
```

- [ ] **Step 3: 验证 README 格式**

```bash
head -60 README.md
```

Expected: 表格正确对齐，5 个新 demo 条目出现。

---

## 自检结果

**规格覆盖：**
- Demo 09 动态技能开关 ✓ Task 1
- Demo 10 MCP Server ✓ Task 2
- Demo 11 MCP Client ✓ Task 3
- Demo 12 跨会话记忆 ✓ Task 4
- Demo 13 心跳监控 ✓ Task 5
- 安装新依赖 ✓ Task 0
- README 更新 ✓ Task 6

**占位符扫描：** 无 TODO/TBD，所有代码步骤均含完整实现。

**类型一致性：**
- `Session.messages: CoreMessage[]` 在 Task 4 定义并使用一致
- `Heartbeat` interface 在 Task 5 定义并使用一致
- MCP Server tool 返回格式 `{ content: [{ type: "text", text: string }] }` 在 Task 2/3 一致
